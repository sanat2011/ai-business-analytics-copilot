"""NL → SQL generation (Phase 5)."""

from __future__ import annotations


def generate_sql(user_question: str, conversation_context: list | None, metadata: dict) -> str:
    raise NotImplementedError("Phase 5 — generate_sql()")
